@model IEnumerable<clinica2.Models.roles>

@{
    ViewData["Title"] = "Index";
}

<h1 style="color: #a35f56;">Роли</h1>

<table class="table">
    <thead>
        <tr>
            <th style="color: #a35f56;">Название роли</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.role_name)
            </td>
        </tr>
}
    </tbody>
</table>
