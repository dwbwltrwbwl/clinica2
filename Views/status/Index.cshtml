@model IEnumerable<clinica2.Models.status>

@{
    ViewData["Title"] = "Index";
}

<h1 style="color: #a35f56;">Статусы</h1>

<table class="table">
    <thead>
        <tr>
            <th style="color: #a35f56;">Название статуса</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.status_name)
            </td>
        </tr>
}
    </tbody>
</table>
