@model IEnumerable<clinica2.Models.diagnosis>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.diagnosis_name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.diagnosis_name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_diagnosis">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_diagnosis">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_diagnosis">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
